Bootstrap: docker
From: ubuntu:24.04

%post
    apt -y update
    apt -y dist-upgrade
    apt -y install python3 python3-pip git zip unzip openjdk-17-jdk autoconf libtool pkg-config
    apt -y install zlib1g-dev libncurses-dev libncursesw6 libtinfo6 cmake libffi-dev libssl-dev
    apt -y install cython3 virtualenv pipx

